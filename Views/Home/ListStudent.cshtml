 @model IEnumerable<StudentManage.Models.Student>

@{
    ViewBag.Title = "ListStudent";
    Layout = "~/Views/Layout/UsingLayout.cshtml";
}

<h2>Список студентов</h2>

<p>
   
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Id Студента")
        </th>
        <th>
            @Html.DisplayName("ФИО")
        </th>
        <th>
            @Html.DisplayName("Дата рождения")
        </th>
        <th>
            @Html.DisplayName("Дата регистрации")
        </th>
        <th>
            @Html.DisplayName("Номер класса")
        </th>
        <th>
            @Html.DisplayName("Id Студента")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdSt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NameSt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BirthdateSt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AddressSt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreateDateSt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdCr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.IdSt }) |
            @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */
       }) |*@
            @Html.ActionLink("Delete", "DeleteStudent", new { id=item.IdSt })
        </td>
    </tr>
}

</table>
